# 后端更新日志

## 2026-08-23

### 主题与插件更新闭环

- 新增主题、插件、任务、审计、事件、资源、健康、锁、请求上下文、URL、菜单和路由公共门面，复用现有任务队列、系统日志、EventBus/Outbox 与健康状态。
- 主题支持本地包登记校验、端面锁、快照激活、资源版本递增、缓存失效和失败回退；插件升级改为 `prepare/confirm` 预检确认流程，返回 `restart_required=true`，不在运行中替换 Python 文件或刷新路由。
- 插件停用、卸载和重新启用接入 owner gate、任务暂停/取消/恢复、事件订阅和运行时资源清理；更新阶段补齐操作审计、生命周期事件和健康快照。
- 全量 16 个插件补齐 Standard manifest，主题 manifest、资源校验和 TDesign 主题令牌规则同步更新；旧插件升级入口保留兼容行为。

### 邮件模板编辑体验

- 邮件模板列表的模板名称支持鼠标悬浮下划线，并可直接进入对应编辑页。
- 邮件模板 TinyMCE 编辑器默认高度由 400px 提升至 500px，增加长内容编辑空间。

## 2026-08-11

### 企业微信通知 3.0.0 管理员动作重构

- 自动通知改用插件自有 15 个管理员动作、Markdown 模板和变量白名单，不再解析邮件 HTML、模板字段或农户收件人。
- 注册、认证、生产、气象和任务失败按源事件单次入队；验证码、密码重置和订单事件过滤验证码与联系方式等敏感变量。
- 升级保留原动作开关、Webhook、正文和发送日志，删除旧规则表及兼容接口，并补齐默认管理员模板。
- 管理页按统一设置页骨架重构四个 Tab，动作配置改为紧凑表格与独立编辑弹窗。
- 通知动作增加与发送设置一致的分类 Tab，启停改为列表行内开关；编辑弹窗聚焦 Markdown 与动作级 Webhook，并统一精简操作按钮图标。

### 小智 AI MCP 1.3.0 数据隔离

- 新增 `hy_plugin_xiaozhi_mcp_server` 插件专属表，升级时保留系统 MCP 并按原 ID 一次性复制存量。
- 小智 CRUD、工具预览和实际调用全部改用插件表，系统 AI 设置继续独立使用原表。
- 工具预览名称列固定为 320px，描述列紧随并在窄屏提供局部横向滚动。

### 统一文件上传设置

- 新增系统设置“上传设置”页，统一维护通用图片、通用文件、资料下载、APK 和 App 广告图策略。
- 新增公共上传策略服务和插件 `get_upload_policy_schema()` 声明协议，按统一值、旧配置、代码默认值顺序兼容读取。
- 头像、聊天图片、通用上传、资料下载、APK 和广告图全部改用统一校验与分块写盘，异常时清理半成品。
- App 管理升级到 1.2.0，文件下载升级到 1.1.0；插件配置改为只读规则摘要并提供统一设置入口。
- 新增管理员上传设置读写接口与农户图片限制接口，上传提示和客户端校验改为动态读取。
- 修复上传规则读取失败时前端继续使用本地默认值的问题，并增加插件卸载并发保存校验。

### 插件同步与热升级

- 应用管理新增全量插件目录同步弹窗，可查看安装版、目录版和更新状态并直接更新。
- 修复热升级重复加载 SQLAlchemy models 导致 App 管理接口被门禁为 404 的问题。

## 2026-08-10

### 小智 AI MCP 1.2.0 自定义源模式

- 删除小智全局系统 MCP API Key 配置，并通过幂等迁移清理遗留配置项。
- 小智工具发现和调用改为只使用管理员自定义 MCP 的缓存工具；调用前复查服务器启用状态和缓存白名单。
- 系统核心和 addon 工具不再直连；需要使用时须将本系统 `/mcp` 作为独立自定义 MCP 配置并测试。
- 管理页调整为“连接与监控 / MCP 管理 / 工具预览”三个顶层 Tabs，旧 `runtime` 链接兼容映射到连接页。
- 工具预览不再依赖 WSS 配置，无服务器、空缓存、停用服务器或损坏缓存均正常返回空列表。
- 管理员自定义 MCP 当前数据按本次升级授权执行一次事务清空，不以迁移重复删除未来配置。

### 智谱 GLM LLM 驱动与视觉辅助

- 将 `vision_glm` 迁移为可激活的 LLM 驱动，支持 GLM-4.6V-Flash 图文直传、工具调用和推理；保留 `vision_glm_analyze` 作为 DeepSeek 的一次性视觉前置。
- 管理员与农户 AI 对话支持图片 Data URL；原图仅存于当前请求，不进入数据库或 SSE。
- 增加图片大小、格式、数量限制及 Key/配额/超时错误脱敏处理。
- 旧视觉插件地址重定向到 AI 设置接口列表；SPA 首屏页面样式纳入导航清理并限定小智/AI 设置 JSON 编辑器选择器作用域。

## 2026-08-09

### 依赖清单整理

- 将运行时依赖同步登记到 `pyproject.toml`，与 `requirements.txt` 保持一致。
- 显式登记直接使用的 `bcrypt` 与 `Jinja2`，避免依赖间接安装。
- 新增 `requirements-dev.txt`，集中管理 Ruff、Mypy、Bandit、Pytest 和 pytest-asyncio。
- 显式登记 `websockets 17.0.1`，用于小智 AI MCP addon 的异步 WSS 长连接。

### 小智 AI MCP 接入插件

- 新增 `xiaozhi_mcp` addon，以单实例 WebSocket 客户端桥接小智远程 MCP 接入点。
- 复用系统个人 MCP API Key、实时账户/RBAC 鉴权和 AI 工具桥，统一发现核心、已启用 addon 与外部 MCP 工具。
- 新增脱敏配置、运行状态、工具预览、临时测试、重连和断开接口及后台页面。
- 工具结果执行 1024 字节限制，工具清单超限时返回明确错误，不截断 JSON。
- 插件禁用与升级流程新增运行时资源清理回调，避免残留并行 WebSocket 连接。
- 插件 MCP Key 失效改为 422 业务错误，避免后台公共 401 处理清除管理员登录态。
- 修复后台页未解包统一响应信封的问题，并在插件安装和页面挂载前刷新动态权限缓存。
- 1.1.0：后台页改为标准 TDesign 卡片、骨架屏、空状态与 Form 内联校验，移除内容区冗余标题和自造页面结构。
- 1.1.0：支持粘贴包含多个 `mcpServers` 的 JSON，幂等导入系统外部 MCP 注册表并自动发现工具。

## 2026-08-05

### MCP 模块安全与稳定性修复

- 修复 `batch_gdd` 跨产区读取批次数据的问题，并统一返回不泄露存在性的错误信息。
- MCP 有效 API Key 改为每次请求实时校验密钥、账户状态和管理员 RBAC 权限；无效 Key 使用最多 1024 条、60 秒的负缓存。
- 未登记 MCP 工具改为默认拒绝，避免绕过受众和权限码过滤。
- 外部 MCP 服务器更新增加 `clear_api_key` 显式清除参数；空 API Key 更新时保留已有凭据。
- 外部 MCP 工具缓存损坏时安全降级为空列表并记录告警。
- 补充跨产区、实时鉴权、负缓存容量、RBAC 缓存绕过、外部凭据和损坏缓存回归测试。

验证：后端全量 pytest 744 项通过；MCP/RBAC 专项 80 项通过；目标文件 Ruff 检查通过。

### 主题预览图修复

- 修正主题清单对 `assets/preview.svg` 的资源探测路径，恢复主题设置页预览图展示。
- 增加主题预览 URL 回归测试。
