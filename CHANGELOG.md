# 后端更新日志

> 自 2026-09-13 起按业务模块汇总更新；日期表示当前开发快照，不等同于生产发布版本。未经过真实数据库、厂商设备或生产环境验收的内容，以源码和专项文档为准。

## 2026-09-19｜3.4.12 版本号升级、任务清理口径回灌与发布副本同步

### 发布与验证

- 应用版本事实源、FastAPI/OpenAPI、健康检查、启动/停机日志、系统 MCP、硬件插件静态资源指纹与 README 统一为 `3.4.12`，不新增数据库迁移或接口结构变更。
- 回灌任务监控手动清理实现：`DELETE /api/admin/v1/task-monitor/logs/cleanup` 改为按 `days`（默认 10）删除最近 N 个自然日内的任务日志、更早历史保留，清理弹窗同步展示窗口起点与影响条数；每日定时保留期清理保持不变。
- 回灌首页仪表盘挂件窄容器重排：挂件网格轨道最小宽度归零，待办磁贴按可用宽度自动换行并在卡片内纵向滚动。
- JJR 详情拍照结果轮询收敛为每 1 秒一次、最多 5 次（5 秒超时），设备回传正常时更快看到新照片，异常时更快给出稍后刷新提示。
- 管理端新增矮窗口滚动兜底（`page-card-scroll` 契约类与 `js/page-scroll.js` 自适应整页滚动），修复窗口变矮时标签页与表格被压缩到 0-2px 后内容不可达的问题。
- 交付副本 release/app 同步当前源码快照并生成 v3.4.12 发布压缩包；插件保持现有版本，历史归档 SQL 与历史发布包保留。

## 2026-09-17｜3.4.11 版本号全局升级与发布副本同步

### 发布与验证

- 应用版本事实源、FastAPI/OpenAPI、健康检查、启动/停机日志、系统 MCP、验证脚本与硬件插件静态资源指纹统一为 `3.4.11`，不新增数据库迁移或接口结构变更。
- 交付副本 release/app 同步当前源码快照并生成 v3.4.11 发布压缩包；插件保持现有版本，历史归档 SQL 与历史发布包保留。

## 2026-09-15｜发布前稳定性修复

### 插件平台与首页磁贴

- 修复停用插件时未捕获停用异常导致的「服务器内部错误」：停用失败统一回滚数据库会话并返回明确的 503 与错误码，预提交失败逐项恢复 owner 门禁与暂停任务。
- 修复插件注册首页磁贴后因停用或缺失导致全局磁贴消失的问题：磁贴布局持久化只增不减、停用仅过滤本次返回值而不改写已保存布局，排序保存保留暂不可用磁贴原位并拒绝清空已配置磁贴。

### 发布与验证

- 修复全量测试在不同事件循环间复用 MySQL 连接池导致的时区用例失败，测试前后均显式释放连接池。
- 将启动种子流程拆分为 `core/startup_seed.py`，保持种子门控和初始化行为不变，并消除 `core/lifespan.py` 文件长度债务。
- 统一 MCP 硬件并行读取结果的异常收口类型，保持部分成功语义并消除 `services/mcp/tools_hardware.py` 的 Mypy 错误。
- 本次不修改外部接口、数据库结构、插件协议或前端路由，发布版本保持 `3.4.1`。

## 2026-09-14｜3.4.1 本地发布与数据库一致性修复

### 插件平台与发布

- 应用版本事实源、FastAPI/OpenAPI、健康检查、静态资源缓存、启动日志和系统 MCP 统一为 `3.4.1`，不新增数据库迁移或接口结构变更。
- 核心归档 SQL 保留为 `3.4.0.sql`，企业微信历史迁移文件移入 `migrations/legacy/`，活动迁移扫描不再读取 4.x/5.x 历史文件。
- 插件包预检、启动加载和数据库体检实际校验 `compatible_app_versions`；当前 3.4.1 源码与本地数据库移除 `sms_idcsmart`。
- 停机插件升级成功后立即刷新数据库体检快照，全量扫描清理已不存在插件的残留快照；本地发布副本和压缩包按 3.4.1 完整快照验收。
- 修复历史“系统核心”虚拟记录被识别为 addon 的问题：启动时清理存量记录，应用列表和插件发现接口均不再展示。

## 2026-09-13｜3.4.0 开发快照（按模块）

### 硬件管理与物联网

- 公共硬件管理统一接入协议来源、设备列表、地块绑定、地图标记、实时/历史数据和详情抽屉；Addon 可同时声明硬件协议与本地详情组件，JJR 详情迁入 `hardware_jjr`，不同来源按稳定字符串 ID 隔离，组件加载失败回退公共详情。
- 实时采集改用物联平台 `/properties` 的最新属性并保留 `/identifiers` 元数据；设备快照按设备唯一保存，周期任务支持列表同步、并发限制、部分失败继续和失败重试，详情自动刷新只读快照，避免缓存缺失时重复请求外部平台。
- 统一设备卡片设置、指标可见性、实时图片/历史图表和拍照能力；植物生长记录仪拍照增加设备类型、状态、权限、频控和操作日志校验，拍照后静默轮询并处理图片缓存刷新。后台地图支持地块内拖动保存标记，农户端仅只读展示。
- 补齐慧眼设备删除与详情修复：删除时同时清理注册信息、公共镜像和实时快照，增加来源串行锁与绑定/存储引擎保护，防止旧同步复活或误删；删除失败保持回滚，允许同 ID 重新注册。
- `hardware_huiyan` 1.0.2 支持仅名称注册并由服务端生成 UUID，注册、逐指标最新值上报和实时查询免 Token；按指标合并并以行锁保护并发，迟到数据不会覆盖新值，管理页支持复制 ID、同步失败重试和调度设置。JJR 继续保持 1.0.1。

### 智能识别

- `yolo_model_manager` 统一为“智能识别”模块，模型、地块绑定、默认识别置信度、不可变识别记录和植物生长记录仪快捷检测形成完整链路；任务提交时冻结置信度，模型或图片变化时生成新的幂等任务键。
- 新增管理员专用 `yolo_model_manager_recognition_analysis(record_id)` MCP Tool：按识别时间查询当前地块绑定硬件的最近历史指标，保留来源、时间差、数据质量和分析边界；插件停用或卸载后立即注销，农户及无权限管理员不可发现或调用。
- 识别记录列表、目标明细和详情图片支持换行及原图预览；图片来源、大小、像素、MIME、内网地址和模型能力均在服务端校验，非视觉模型明确拒绝图片。

### AI 对话与 AgentScope

- AgentScope 2.0.7 资源工作台作为 AI 运行时唯一事实源，切换至 `agents`、`credentials`、`sessions`、`messages`、`schedules`、`teams`、`mcps`、`skills` 等表；旧 `hy_ai_*` 数据不迁移、不回填。
- 启用中的 MCP 在每次 Workspace 解析时自动收敛到管理员全部新旧会话，Skill 仍按会话显式安装；`ModelCard.support_tools` 和 `input_types` 分别作为工具、图片能力硬门禁，平台 Write/Edit/Shell、计划、调度和团队工具不注入聊天。
- 新增按次思考入口 `/api/ai/chat/turn`，思考开关和等级只作用于当前任务模型实例，不写入会话或消息；DeepSeek、GLM、OpenAI Chat/Responses 按各自协议映射参数。GLM 目录扩充至近期 5 系列、4.7/4.6 文本及视觉模型，刷新失败保留最近一次成功目录。
- 本地聊天图片统一使用受控内部引用，模型调用副本临时 Base64 内联，公网 HTTPS 图片保持 URL 投递；缩略图限制在 `180×160px` 内并支持 TDesign ImageViewer，最多 4 张，非视觉模型拒绝图片。
- SSE 消费改为按绘制帧排空并保留结构事件，首个可见内容后释放消息骨架；会话新建、切换和重连等待就绪事件后才发送，遗留确认/asking 事件自动拒绝并恢复。进程退出或 reload 时主动排空 SSE，避免长连接阻塞。

### 系统 MCP 与小智连接桥

- 系统 MCP 恢复农业主链 6 个只读 Tools、5 个 JSON Resources 和 2 个 Prompts，并纳入硬件查询与智能识别预览/确认/结果摘要；管理员按 RBAC，农户按绑定产区和批次范围隔离，未知能力默认拒绝。
- Tools、Resources、Prompts 统一使用个人 API Key 实时鉴权和每账户 `120 次/60 秒` 频控，超限返回重试秒数；发现和调用结果统一压缩，限制描述、参数和结果体积，日志不记录密钥、正文或完整参数。
- 系统 MCP 地址统一规范为 `/mcp/`，存量 `/mcp` 在装配前幂等修正，避免重定向丢失认证头；管理员启用、停用、删除或修改 MCP 后，下一轮会话解析自动同步，单个外部服务不可达只撤回自身工具并在下轮重试。
- `xiaozhi_mcp` 维护 WSS 连接、连接监控、自定义 MCP 和缓存工具预览；与 AgentScope Storage 物理隔离，不再保存全局系统 Key，也不直接复制旧 AI 或 addon 工具。

### 对象存储、文件上传与迁移

- 新增七牛云对象存储插件，支持 AccessKey/SecretKey、空间、域名、区域、公私有模式、保存路径、连通检测、前缀分页、上传、访问地址和删除；通过 ETag 一致性检查区分相同、缺失、冲突和不支持。
- `oss_service` 成为统一存储调度层：业务只保存稳定文件地址，访问时按当前插件动态生成公开直链或私有签名；远端不可用时回落本地副本，兼容历史 `object_key` 和路径变更。
- 公共文件迁移改为一键扫描后自动入队，活动任务复用、旧 awaiting 任务自动恢复；每批最多 10 个文件、并发 2、失败最多重试 3 次，成功后再更新文件日志和业务引用，本地副本始终保留。进度改在管理端顶部栏全局只读展示，切换页面不丢失。
- 统一头像、聊天图片、资料下载、APK 和广告图的上传策略、扩展名/大小校验、UUID 命名及 `upload/common` 落盘，禁止把 Base64、临时签名地址或本机绝对路径写入业务数据。

### 插件平台与数据库维护

- Addon 安装时将非隐藏后台页面幂等加入“应用”侧边栏，处理父菜单缺失、页面键冲突和失败回滚；启停、卸载、升级统一按 owner 注销 Router、Event、Pipeline、Task、MCP、Page、Widget 等运行时能力。
- 插件更新继续采用预检、确认、停机应用、原子替换、迁移、失败恢复和健康检查闭环；Web 进程不直接替换文件或执行迁移，已确认计划由本地控制中心或离线脚本执行。
- 新增插件数据库体检：按 1/6/12/24 小时扫描 manifest、`hy_plugin.version` 以及声明式表/列/索引，保存每个插件最新状态快照；同版本结构漂移通过插件幂等修复钩子处理，统一在后端停机后提交或回滚。
- 统一插件上传策略、主题资源、页面壳、静态资源命名空间和版本缓存标识；保留本地 vendor，明暗主题继续通过三端 `data-theme` 提供。

### 天气、任务与仪表盘

- 修复和风天气历史自动回补：历史接口改用 LocationID 与 `yyyyMMdd`，仅写入有效日温且不覆盖已有记录；按产区独立事务处理，单日失败继续后续日期并在任务日志中保留原因，权限失败不以预报数据替代。
- 任务平台继续复用统一队列、Outbox、重试、死信和监控；天气回补、硬件采集、模型检测和对象存储迁移均有明确 owner、幂等键和失败隔离。
- 仪表盘新增/完善在线硬件、最近识别记录、最近知识库记录和政策资讯挂件，支持管理员显隐/排序持久化、农户只读范围、空数据降级和明暗主题。

### 本地控制中心与运维

- 本地控制中心固定监听 `127.0.0.1:8765`，提供后端启动、停止、重启、健康状态、滚动日志和插件更新进度；使用启动令牌、HttpOnly 会话、Origin/CSRF 校验、单操作互斥及外部 `8000` 端口保护。
- Windows 监督进程只替换业务 Uvicorn，控制中心和日志保持可用；插件更新按停止、预检、应用、重启、健康检查五阶段展示，失败自动恢复并保留审计记录。生产环境不开放控制中心。

### 管理端 Web 与主题

- 管理端和农户端继续统一 TDesign Vue Next、本地 vendor、共享请求/布局组件及明暗主题；硬件管理页增加卡片/列表切换、分页、筛选保留、空态/加载态和详情资源释放。
- Web 验收基线统一为 `1280×900` PC 桌面视口，SPA 往返后保持 Tab、筛选和任务进度；不新增手机、平板或小屏断点。

## 历史更新（按日期）

### 2026-09-07

#### 慧眼物联网插件 1.0.2

- 设备仅名称注册并返回UUID；公开注册、上报和查询免Token，管理员权限保留。
- 新增逐指标最新值上报与查询，行锁保护并发合并，迟到数据不覆盖新值。
- 管理页提供接入示例、ID复制及同步重试，兼容已有设备与配置入口；系统3.4.0、JJR1.0.1不变。

### 2026-09-06

#### 硬件多来源与详情组件

- Addon 同时声明硬件协议与本地详情组件，公共层保留列表、绑定、外观和抽屉。
- JJR 协议与卡片迁入 hardware_jjr，设备按来源与稳定字符串 ID 隔离，支持不同厂商同时接入。
- 停用门禁和运行代次保护采集结果；有关联设备禁止卸载，组件加载失败回退公共详情。
- 增加停机迁移预检与幂等应用、隔离 MySQL 及 Edge 桌面模拟验收。

### 2026-08-31

#### 运行基线统一

- 当前应用版本统一为 `3.4.0`，Python 运行基线统一为 `3.12`。
- 生产部署补充 1Panel Docker 编排指南；历史迁移文件中的版本号保持原样。

### 2026-08-30

#### Addon 页面自动加入应用侧边栏

- Addon 安装事务会将全部非隐藏后台页面追加到“应用”父目录，前台页面和隐藏辅助页不进入后台侧边栏。
- 页面导航与菜单按插件归属幂等注册，父菜单缺失、不唯一或页面键被其他来源占用时安装失败并完整回滚。
- 现有已安装插件布局不回填；管理员仍可在导航管理中调整安装后生成的菜单，卸载继续统一清理插件归属记录。

#### 智能识别插件 1.1.0 联合硬件分析 MCP

- 新增管理员只读 `yolo_model_manager_recognition_analysis(record_id)`，使用 `yolo_model_manager:analysis` RBAC 权限；插件停用或卸载后即时注销，农户不可发现或调用。
- 新增公共硬件历史最近值门面：按识别记录地块的当前绑定设备和当前指标清单，分别定位识别时间前后候选值，绝对时间差相同时优先之前值；全部物联请求最多 5 路并发、60 秒总预算，失败保留脱敏部分结果。
- Tool 返回识别事实、硬件事实、精确时间差、数据质量和模型分析边界；不返回写入管理员、任务内部信息、设备坐标、物联凭据、原始异常或图片 Base64，不触发实时采集或快照更新。
- 插件版本升级至 1.1.0，无数据库迁移；升级钩子幂等同步新权限和 AgentScope 工具策略。

#### 系统 MCP 自动装配修复

- 系统 MCP 预设和管理接口统一使用 `/mcp/` 规范地址；存量 `/mcp` 配置在会话装配前幂等规范化，避免 AgentScope 不跟随 ASGI `307` 时工具发现失败，外部 MCP 地址不受影响。
- 运行时 Agent 提示升级为 v3，明确启用中的 MCP 自动进入 Toolkit 并由模型自主调用，不再要求手动绑定会话、天气资源或在未调用工具时推断未绑定产区。
- 增加旧地址、Header 保留/清除、幂等装配和旧提示升级回归测试，并修复默认 Firefox 冒烟环境的剪贴板夹具兼容性。

#### MCP 启用即全会话可用

- 取消 AI 设置页的 MCP Agent/Session 手动绑定；管理员保存并启用 MCP 后，在下一轮对话自动进入该管理员全部新旧会话，停用、删除及 URL/Headers 更新自动收敛。
- Workspace 同步按用户、Workspace、Agent、Session 加锁并保持幂等；单个 MCP 不可达时只撤回其工具并记录脱敏错误，下轮重新尝试，不影响其他 MCP 或普通对话。
- Workspace MCP 接口继续保留兼容，运行时以资源所有者 MCP 资源库和 `enabled` 为唯一事实；Skill 显式安装、ModelCard 工具门禁及平台工具过滤保持不变。
- 验证结果：AgentScope/MCP 专项 112 项、后端全量 1010 项通过；`1280x900` 明暗主题桌面冒烟通过。统一 lint 的 Ruff、文件长度、Mypy 和 Bandit 通过，仅保留任务前已有的 `ai-chat.html` 两处 Design Token 告警。

#### 系统内 MCP 基础能力一期

- 恢复农业主链 6 个只读 Tools，新增 5 个 JSON Resources 与 2 个 Prompts；管理员按 RBAC、农户按绑定产区和批次归属隔离，未知能力默认拒绝。
- 三类能力统一复用个人 API Key 实时鉴权，并按账户限制为每 60 秒 120 次；频控响应返回 `-32001` 和建议重试秒数，日志不记录密钥、资源正文或完整参数。
- AI 资源页新增“接入系统 MCP”预设，个人 Key 仅生成 Bearer Header；编辑留空保留已存 Headers，显式提交空对象才清除，列表及保存响应均不回显凭据。
- 验证结果：后端全量 pytest 1004 项通过，桌面浏览器冒烟 25 项通过；统一 lint 的 Ruff、文件长度、Mypy 和 Bandit 通过，仅剩用户既有 `ai-chat.html` 两处 Design Token 风格告警。

#### 智谱 GLM 近期模型目录

- AgentScope GLM Provider 从 2 个模型扩充为 11 个近期 Chat Completions 模型，覆盖 GLM 5 系列、4.7/4.6 文本模型及 GLM-5V/4.6V 视觉模型。
- GLM 模型发现继续使用本地已验证目录，视觉能力仅由模型输入模态声明，旧专项模型和已弃用型号不进入新建 ModelCard 候选。

#### AI 对话按次思考与图片预览

- 新增 `/api/ai/chat/turn`，按当前会话启用中的 ModelCard 校验思考能力和等级，并使用任务级上下文把参数限定在本轮模型实例；Session、永久消息和后续调用不保存本轮选项，原生 `/chat/` 继续承载确认与恢复事件。
- ModelCard 列表新增 Schema 派生的 `reasoning_efforts` 和 `default_reasoning_effort`；DeepSeek 支持 `high/max`，调用时生成 `thinking.type=enabled|disabled` 和对应 `reasoning_effort`，GLM、OpenAI Chat/Responses 使用各自原生参数映射。
- 管理端 ChatSender 增加默认关闭的“深度思考”开关和动态等级选择器，发送成功、切换模型或切换会话后重置，失败保留供重试；图片缩略图改为最大 `180×160px` 内等比显示并支持 TDesign ImageViewer 点击预览。

### 2026-08-29

#### AI 对话本地图片统一修复

- 聊天图片上传新增 `model_url`：本地文件使用受控 `huiyan-upload://common/<UUID文件名>`，公网 HTTPS 对象地址保持不变；浏览器仍使用 `url` 预览。
- 模型调用前在消息深拷贝中校验本地文件目录、名称、MIME、签名、大小和四图上限并临时 Base64 内联，永久会话不保存文件内容或本机绝对路径；非视觉模型明确拒绝图片。
- 视觉 DeepSeek 增加兼容 `image_url` 的格式化器，GLM、OpenAI Chat/Responses 沿用原生多模态格式；ModelCard 改由 `input_types` 派生视觉能力并通过幂等迁移修复存量矛盾记录。

#### 智能识别插件 1.0.4

- 模型管理新增 1% 到 100% 的默认识别置信度配置，覆盖上传、编辑和列表展示，并通过 1.0.4 迁移为现有模型补入 25% 默认值。
- 快捷检测任务在提交时冻结模型置信度并传入 Ultralytics 推理，同一图片在置信度变化后使用新的幂等任务键。
- 快捷检测结果改为可换行的纵向目标明细并支持原图点击预览，识别记录详情图高度同步增加。

#### 慧眼本地控制中心 1.0

- 新增独立于业务 Uvicorn 的本地控制中心，提供后端启动、停止、重启、健康状态和滚动日志。
- 插件更新改为可视化停止、离线预检、正式应用、重启与健康检查流程，内部继续调用既有离线更新器。
- 新增启动令牌、HttpOnly 会话、Origin/CSRF 校验、单操作互斥及外部 8000 端口保护。
- 应用管理页为待重启计划增加控制中心入口，并提供 Windows 一键启动器和桌面快捷方式安装脚本。
- Windows 使用独立监督进程管理业务 Uvicorn 热重载，源码变化只替换业务进程，控制中心页面和日志持续可用。

#### 智能识别插件 1.0.3

- 植物生长记录仪实时页新增按插件启用状态和权限显示的“快捷检测”，使用当前最新成功图片快照，不额外下发拍照指令。
- 新增检测上下文、MySQL 队列任务提交和本人任务状态接口；任务通过地块当前绑定的 PT/ONNX 模型在 CPU 上执行，并将目标框、原图尺寸和设备来源写入不可变识别记录。
- 图片来源由服务端解析并冻结，增加 HTTP(S) 内网地址、响应类型、20MB 大小、像素上限和站内 `/upload/` 路径校验；绑定变化或模型文件丢失时明确失败。
- 识别记录表增加任务关联、来源设备、图片标识和原图尺寸快照，硬件服务新增不暴露 ORM 的设备 DTO 与只读缓存快照门面。

#### 智能识别插件 1.0.2

- `yolo_model_manager` 展示名称统一调整为“智能识别”，管理页面改为默认识别记录与模型管理双 Tab，并保持 URL Tab 状态。
- 新增不可变识别记录表和管理员写入、分页筛选、筛选选项、详情四个接口，保存地块与模型名称快照、目标明细、图片地址和中国标准识别时间。
- 写入接口要求模型为地块当前绑定模型，校验安全图片地址、置信度和目标框；空目标数组作为正常识别结果保存。
- 插件版本轨道调整为 `1.0.0 -> 1.0.1 -> 1.0.2`，1.0.2 迁移新增识别记录表并保留现有模型、标签与地块绑定数据。

#### 硬件实时属性与植物生长记录仪拍照

- 实时采集接入物联平台 `/properties`，保留 `/identifiers` 的名称、单位和类型元数据，并以最新属性覆盖值；新接口不可用时继续使用原标识值，避免中断已有快照与自动采集。
- 管理端新增植物生长记录仪拍照代理，限制设备类型、可用状态、权限和发送频率，并记录管理员操作日志；硬件详情实时数据页新增拍照入口。
- 拍照指令下发后每 2 秒静默刷新实时属性，检测图片地址或更新时间变化后自动更新，最长等待 20 秒；实时图片追加版本参数，兼容平台复用同一图片地址时的浏览器缓存。
